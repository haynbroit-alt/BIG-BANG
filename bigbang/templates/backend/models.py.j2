# Generated by BIG BANG — {{ universe.name }}
# Content between @bigbang markers is regenerated automatically.
# Add your custom models OUTSIDE the markers — they will never be touched.

# @bigbang:start:imports
from datetime import datetime

from sqlalchemy import Boolean, Column, DateTime, Float, Integer, String, Text, func

from database import Base
# @bigbang:end:imports

{% for entity in universe.entities %}
# @bigbang:start:{{ entity.name | slug }}_model

class {{ entity.name }}(Base):
    __tablename__ = "{{ entity.name | slug }}s"

    id = Column(Integer, primary_key=True, index=True, autoincrement=True)
{% for field in entity.fields %}
{% if not field.computed %}
    {{ field.name }} = Column({{ field.type | sa_type }}{% if field.required %}, nullable=False{% else %}, nullable=True{% endif %})
{% endif %}
{% endfor %}
    created_at = Column(DateTime, default=datetime.utcnow, server_default=func.now())
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
# @bigbang:end:{{ entity.name | slug }}_model

{% endfor %}
# ── Custom models (preserved across regenerations) ────────────────────────────
