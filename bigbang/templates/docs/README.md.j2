# {{ universe.name }}

> Generated by **BIG BANG** — The Universe Generator.
> One `genesis.yaml`. One command. A complete production-ready application.

## Overview

| | |
|---|---|
| **Name** | {{ universe.name }} |
| **Type** | {{ universe.type }} |
| **Entities** | {% for e in universe.entities %}{{ e.name }}{% if not loop.last %}, {% endif %}{% endfor %} |
| **Flows** | {{ universe.flows | map(attribute='name') | join(', ') or 'none' }} |
{% if universe.monetization %}| **Billing** | {{ universe.monetization.model }} |{% endif %}

## Quick Start

```bash
# 1. Configure secrets
cp .env.example .env
# Edit .env with your values

# 2. Launch the universe
docker-compose up -d

# 3. Explore
open http://localhost:3000        # Dashboard
open http://localhost:8000/docs   # API (Swagger)
```

## API Reference

**Base URL**: `http://localhost:8000/api/v1`

{% for entity in universe.entities %}
### {{ entity.name }}

| Method | Endpoint | Description |
|--------|----------|-------------|
| `GET` | `/{{ entity.name | plural }}` | List all |
| `POST` | `/{{ entity.name | plural }}` | Create |
| `GET` | `/{{ entity.name | plural }}/{id}` | Get one |
| `PUT` | `/{{ entity.name | plural }}/{id}` | Update |
| `DELETE` | `/{{ entity.name | plural }}/{id}` | Delete |

**Fields:**

| Name | Type | Required | Computed |
|------|------|----------|----------|
{% for field in entity.fields %}
| `{{ field.name }}` | `{{ field.type }}` | {{ '✓' if field.required else '—' }} | {{ '✓' if field.computed else '—' }} |
{% endfor %}

{% endfor %}
{% if universe.flows %}
## Workflows

{% for flow in universe.flows %}
### {{ flow.name }}

**Trigger**: `{{ flow.trigger }}`

```http
POST /api/v1/flows/{{ flow.name }}
```

| Step | Action |
|------|--------|
{% for step in flow.steps %}
| {{ loop.index }} | `{{ step.action }}` |
{% endfor %}

{% endfor %}
{% endif %}
{% if universe.monetization %}
## Pricing

**Model**: {{ universe.monetization.model }}

| Plan | Price |
|------|-------|
{% for plan in universe.monetization.plans %}
| {{ plan.name }} | {{ plan.price }} {{ plan.currency }}/mo |
{% endfor %}

**Stripe integration**: set `STRIPE_SECRET_KEY` in `.env` and wire up the
`/api/v1/subscribe/{plan}` endpoint in `backend/routes.py`.

{% endif %}
## Project Structure

```
.
├── backend/
│   ├── app.py          # FastAPI application & lifespan
│   ├── database.py     # SQLAlchemy engine & session
│   ├── models.py       # ORM models
│   ├── schemas.py      # Pydantic request/response schemas
│   ├── routes.py       # API routes & flow endpoints
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── index.html      # Single-page dashboard
│   └── static/
│       ├── app.js      # Vanilla JS — no build step needed
│       └── styles.css
├── docker-compose.yml
├── .env.example
└── README.md
```

---

*Generated by [BIG BANG](https://github.com/haynbroit-alt/BIG-BANG) — The Universe Generator*
